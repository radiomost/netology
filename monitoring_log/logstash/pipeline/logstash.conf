input {
  tcp {
    port => 5000
    codec => json
  }

  beats {
    port => 5044
  }
}

output {
  elasticsearch {
    hosts => ["http://es-hot:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }

  stdout { codec => rubydebug }
}